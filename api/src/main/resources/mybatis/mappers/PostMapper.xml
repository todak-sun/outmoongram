<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.highdk.api.v1.post.PostDao">
	<insert id="create" useGeneratedKeys="true" keyProperty="id" parameterType="me.highdk.api.v1.post.Post">
		INSERT
			INTO
			OUTMOON_POST (
				CONTENT,
				UPDATED_AT,
				WRITTEN_AT,
				WRITER_ID
			)
		VALUES (
			#{content},
			now(),
			now(),
			#{writerId}
		);
	</insert>
	
	<select id="getById" parameterType="java.lang.Long" resultType="me.highdk.api.v1.post.Post">
		SELECT
			ID,
			CONTENT,
			WRITTEN_AT,
			UPDATED_AT,
			LIKE_CNT,
			COMMENT_CNT,
			WRITER_ID
		FROM
			OUTMOON_POST
		WHERE
				ID = #{id}
			AND is_used = TRUE;
	</select>
	
	<select id="findById" parameterType="java.lang.Long" resultType="me.highdk.api.v1.post.Post">
		SELECT
			ID,
			CONTENT,
			WRITTEN_AT,
			UPDATED_AT,
			LIKE_CNT,
			COMMENT_CNT,
			WRITER_ID
		FROM
			OUTMOON_POST
		WHERE
				ID = #{id}
			AND is_used = TRUE;
	</select>
	
	<select id="findAll" resultType="me.highdk.api.v1.post.Post">
		SELECT 
			id,
			content,
			written_at ,
			updated_at ,
			like_cnt ,
			comment_cnt ,
			is_used ,
			writer_id 
		FROM outmoon_post
		WHERE  is_used = TRUE
		ORDER BY ID
		OFFSET ${pageDto.start}
		LIMIT ${pageDto.size};
	</select>
	
	<select id="countTotal" resultType="java.lang.Long">
		SELECT 
			COUNT(*)
		FROM outmoon_post;
	</select>
	
	<update id="update" parameterType="me.highdk.api.v1.post.Post">
		UPDATE	outmoon_post
		SET		content = #{content},
				updated_at = now()
		WHERE	id = #{id};
	</update>
	
	<update id="deleteByFlag">
		UPDATE	outmoon_post 
		SET		is_used = FALSE 
		WHERE	id = #{postId};
	</update>
	
</mapper>